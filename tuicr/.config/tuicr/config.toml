# tuicr configuration
# Tinted theme is generated by tinty (via tinty-tuicr-theme.sh) and lives in themes/tinted.toml.
# When you switch tinty schemes, run `tinty apply <scheme>` to regenerate this theme.

theme = "tinted"
diff_view = "side-by-side"
